export enum MessageChannel { SMS = 'sms', TELEGRAM = 'telegram', EMAIL = 'email', PUSH = 'push' }
export enum CampaignStatus { DRAFT = 'draft', SCHEDULED = 'scheduled', RUNNING = 'running', SENT = 'sent', CANCELLED = 'cancelled' }
export enum DeliveryStatus { QUEUED = 'queued', SENT = 'sent', FAILED = 'failed', DELIVERED = 'delivered' }
