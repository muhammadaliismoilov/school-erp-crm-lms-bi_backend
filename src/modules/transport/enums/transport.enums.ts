export enum VehicleStatus { ACTIVE = 'active', MAINTENANCE = 'maintenance', INACTIVE = 'inactive' }
export enum RouteStatus { ACTIVE = 'active', PAUSED = 'paused', ARCHIVED = 'archived' }
export enum TripStatus { PLANNED = 'planned', STARTED = 'started', COMPLETED = 'completed', CANCELLED = 'cancelled' }
