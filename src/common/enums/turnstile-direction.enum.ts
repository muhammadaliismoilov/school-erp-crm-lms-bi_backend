/** Turniket qurilmasining o'tish yo'nalishi. */
export enum TurnstileDirection {
  /** Faqat kirish nuqtasi. */
  IN = 'in',
  /** Faqat chiqish nuqtasi. */
  OUT = 'out',
  /** Ikki tomonlama (yo'nalish har hodisada belgilanadi). */
  BOTH = 'both',
}
